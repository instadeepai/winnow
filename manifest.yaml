# # yaml-language-server: $schema=https://instadeep.aichor.ai/schema/latest/manifest.schema.json
# kind: AIchorManifest
# apiVersion: 0.2.1
# builder:
#   image: winnow
#   dockerfile: ./Dockerfile
#   context: .
#   buildArgs:
#     USE_CUDA: "true"

# spec:
#   debug:
#       vscode:
#         enabled: true # required
#         path: "/aichor/code" # Optional, defaults to "code". Refer the absolute path of the binary if not findable in $PATH
#         provider: "github" # Optional, vscode tunnel provider. Options are "github" or "microsoft", defaults to "github"
#   operator: jobset
#   image: winnow
#   command: make compute_train_features

#   types:
#     worker:
#       count: 1
#       resources:
#         cpus: 6
#         ramRatio: 8 # >= 2


# yaml-language-server: $schema=https://instadeep.aichor.ai/schema/latest/manifest.schema.json
# AIChor manifest for experiments that need a self-hosted Koina inference server.
# Triton/Koina runs in the same pod as the winnow worker (started by entrypoint.koina.sh)
# and is reached over localhost:8500. Override `spec.command` per experiment.
kind: AIchorManifest
apiVersion: 0.2.1
builder:
  image: winnow-koina
  dockerfile: ./Dockerfile.koina
  context: .
  buildArgs:
    USE_CUDA: "true"

spec:
  debug:
      vscode:
        enabled: true
        path: "/aichor/code"
        provider: "github"
  operator: jobset
  image: winnow-koina
  # AIChor overrides the Docker ENTRYPOINT, so we must invoke the entrypoint
  # script explicitly to start Triton/Koina before running the make target.
  command: /usr/local/bin/entrypoint.koina.sh make compute_features_acpt && make compute_features_massivekb

  types:
    worker:
      count: 1
      resources:
        cpus: 6
        ramRatio: 9 # >= 2

        accelerators:
          gpu:
            count: 0
            type: gpu
            product: NVIDIA-H100-80GB-HBM3-MIG-3g.40gb
