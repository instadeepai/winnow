# yaml-language-server: $schema=https://instadeep.aichor.ai/schema/latest/manifest.schema.json
kind: AIchorManifest
apiVersion: 0.2.1
builder:
  image: winnow-koina
  dockerfile: ./Dockerfile.koina
  context: .
  buildArgs:
    USE_CUDA: "true"

spec:
  debug:
      vscode:
        enabled: false
        path: "/aichor/code"
        provider: "github"
  operator: jobset
  image: winnow-koina
  command: /usr/local/bin/entrypoint.koina.sh make make hpo-pipeline

  types:
    worker:
      count: 1
      resources:
        cpus: 12
        ramRatio: 9 # >= 2

        accelerators:
          gpu:
            count: 1
            type: gpu
            product: NVIDIA-H100-80GB-HBM3-MIG-3g.40gb
