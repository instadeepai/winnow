# # yaml-language-server: $schema=https://instadeep.aichor.ai/schema/latest/manifest.schema.json
# kind: AIchorManifest
# apiVersion: 0.2.1
# builder:
#   image: winnow
#   dockerfile: ./Dockerfile
#   context: .
#   buildArgs:
#     USE_CUDA: "true"

# spec:
#   debug:
#       vscode:
#         enabled: true # required
#         path: "/aichor/code" # Optional, defaults to "code". Refer the absolute path of the binary if not findable in $PATH
#         provider: "github" # Optional, vscode tunnel provider. Options are "github" or "microsoft", defaults to "github"
#   operator: jobset
#   image: winnow
#   command: make compute_train_features

#   types:
#     worker:
#       count: 1
#       resources:
#         cpus: 6
#         ramRatio: 8 # >= 2


# yaml-language-server: $schema=https://instadeep.aichor.ai/schema/latest/manifest.schema.json
# AIChor manifest – Optuna HPO for the calibrator (no Koina required).
kind: AIchorManifest
apiVersion: 0.2.1
builder:
  image: winnow
  dockerfile: ./Dockerfile
  context: .
  buildArgs:
    USE_CUDA: "true"

spec:
  debug:
      vscode:
        enabled: false
        path: "/aichor/code"
        provider: "github"
  operator: jobset
  image: winnow
  command: make hpo && make hpo-upload-model

  types:
    worker:
      count: 1
      resources:
        cpus: 6
        ramRatio: 9 # >= 2

        accelerators:
          gpu:
            count: 1
            type: gpu
            product: NVIDIA-H100-80GB-HBM3-MIG-3g.40gb
