# yaml-language-server: $schema=https://instadeep.aichor.ai/schema/latest/manifest.schema.json
kind: AIchorManifest
apiVersion: 0.2.1
builder:
  image: winnow
  dockerfile: ./Dockerfile
  context: .
  buildArgs:
    USE_CUDA: "true"

spec:
  debug:
      vscode:
        enabled: false # required
        path: "/aichor/code" # Optional, defaults to "code". Refer the absolute path of the binary if not findable in $PATH
        provider: "github" # Optional, vscode tunnel provider. Options are "github" or "microsoft", defaults to "github"
  operator: jobset
  image: winnow
  command: make set-gcp-credentials && make download-beams && make download-spectra && make train-general-model && make upload-model && make upload-training-results && make evaluate-general-model && make upload-evaluation-results

  types:
    worker:
      count: 1
      resources:
        cpus: 8
        ramRatio: 8 # >= 2
